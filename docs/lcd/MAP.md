# MAP — Apple1JS

> **Logical-organization guardrails.** Read this FIRST on any cold start, before planning new
> work. It exists so the AI (and you) don't re-derive the project's shape every session — and
> don't reinvent a place that already exists. Generated by `/lcd:onboard` from the _actual_
> structure; refined by `lcd:refine` when reality drifts. Keep under ~150 lines.

**Project type:** software
**Generated:** 2026-05-28 · **Last refined:** 2026-08-06

---

## Shape

Browser-based, cycle-accurate Apple 1 emulator (TypeScript + React, built with Vite). The 6502 CPU
is a **dual engine**: a TypeScript reference implementation and a Rust→WASM implementation
(`wasm-cpu/` compiled via `wasm-pack` into `src/wasm/`), switchable at runtime with the JS engine
as the always-available fallback/reference oracle. Layered architecture: a framework-free emulation
core, a worker-hosted system-integration layer, and a React UI that consumes inspectable state.

## Zones / Components

> One row per top-level area. "Don't put here" is the guardrail that prevents sprawl.

| Zone                               | Path                                                                                | Responsibility                                                                                       | Don't put here                                                      |
| ---------------------------------- | ----------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| core                               | `src/core/`                                                                         | Emulation engine: CPU, Bus, Memory, Clock — pure 6502/hardware logic                                 | React, DOM, worker glue, framework deps                             |
| apple1                             | `src/apple1/`                                                                       | System integration, board assembly, the Worker entry, save/load, typed worker-message defs           | UI rendering, pure CPU logic                                        |
| wasm-cpu                           | `wasm-cpu/`                                                                         | Rust 6502 source compiled to WASM (the second engine)                                                | TS logic; JS-only behavior                                          |
| wasm                               | `src/wasm/`                                                                         | **Generated** wasm-pack bindings (`yarn wasm:build`)                                                 | hand edits — it's regenerated                                       |
| components                         | `src/components/`                                                                   | React UI (presentational); consumes the inspectable tree                                             | emulation logic, `IVersionedStatefulComponent`/`IInspectable` impls |
| services                           | `src/services/`                                                                     | LoggingService, worker communication, state persistence                                              | domain/CPU logic                                                    |
| contexts / hooks                   | `src/contexts/`, `src/hooks/`                                                       | React state management & reusable patterns                                                           | emulation logic                                                     |
| constants / config / types / utils | `src/constants/`, `src/config.ts`, `src/types/`, `src/utils/`                       | Shared constants, config, types, helpers (incl. `Formatters`)                                        | feature logic                                                       |
| styles                             | `src/styles/`                                                                       | Design-token SSOT (`tokens.ts`) + the `tailwind-tokens.ts` adapter, guarded by the token-parity test | hardcoded colors; per-component CSS                                 |
| tests                              | `src/**/__tests__/`, `src/__integration__/`, `src/test-support/`, `src/test-utils/` | Co-located unit tests, integration tests, shared test scaffolding                                    | production code in `test-*`                                         |
| scripts                            | `scripts/`                                                                          | Build/diagnostic helpers (`check-wasm-build.js`, `diagnose-cpu.mjs`)                                 | app or emulation code                                               |
| claude                             | `.claude/`                                                                          | Agent config: `rules/`, `agents/`, `commands/`, `skills/`                                            | project docs — those go in `docs/`                                  |
| docs                               | `docs/` (`active/`, `lcd/`)                                                         | Architecture/roadmap/perf docs (`docs/active/`), LCD artifacts (`docs/lcd/`)                         | code                                                                |

## Entry points

- **Dev:** `yarn dev` → `wasm:build:release` then `vite` → boots `index.html` → `src/index-web.tsx`
- **Dev (no WASM rebuild):** `yarn dev:vite` — reuses the prebuilt `src/wasm/`; the right one for
  CSS/markup-only work.
- **Build:** `yarn build` → `wasm:build:release` + `tsc` + `vite build`
- **WASM:** `yarn wasm:build:release` (`cd wasm-cpu && wasm-pack build --release --target web --out-dir ../src/wasm`)
- **Tests / gate:** `yarn test:ci` (lint + type-check + `vitest run`). Note `pretest` means plain
  `yarn test` also runs lint + type-check — but `test` is `vitest --silent` (watch-ish), not a
  one-shot run; use `vitest run` forms for anything automated.
- **Markdown lint:** `yarn lint:md` (read-only). Never the repo-wide `lint:md:fix` — see Invariants.

## Surfaces in use

`RENDER` (the CRT/terminal display + debugger UI) and `none` (internal emulation logic / invariants).
No `CLI` / `HTTP` / `MCP` / `DB` surfaces. The headless `BENCH=1` throughput harness is a measurement
tool, not a product surface.

## Invariants (always-true rules for THIS project)

> Recurring rules that hold across all work here (LCD principle: don't relitigate them each
> time). The Deep lane's Constitution check and the red-green loop honor these.

- **Never commit to `master`** — always a feature branch (`feat/ fix/ refactor/ docs/ test/ chore/`).
- **Dual-engine parity** — JS and WASM engines must stay feature-equal; test **both** for every
  engine change. The JS engine is the reference oracle / fallback.
  _Where that verification happens:_ in a **browser**, not CI. Native `cargo test` panics on the
  `CPU6502::new()` / `WasmSystem::initialize()` path (wasm-bindgen imports), so `yarn wasm:test`
  covers only the pure `bus.rs`/`ram.rs`/`rom.rs` components; the Node parity suites `skipIf` and
  skip in CI. A Rust core change gets `cargo check` + a skipped parity test — the real check is
  manual. See `docs/active/cpu_test_guidelines.md`.
- **No `any`** (strict mode), **no `console.log`** (use `LoggingService`), **no hardcoded colors**
  (design tokens — except the CRT display).
- **Bump `src/version.ts`** before every PR (single source of truth; patch/minor/major per branch prefix).
- **Stateful/inspectable contracts** (`IVersionedStatefulComponent`, `IInspectableComponent`) are
  scoped to the **core/IO layer**, not React presentational components.
- **WASM build is speed-first** (`opt-level = 3`, ~155 KB); don't restore the abandoned size-first
  (<100 KB) target without approval.
- **markdownlint is per-file only** — `npx markdownlint-cli2 --fix "<file.md>"` on the files you
  actually edited. **Never** the repo-wide `yarn lint:md:fix`: it corrupts the machine block in
  `.claude/rules/lcd-conventions.md` and reflows nested lists (prettier wants 4-space, MD007 wants
  2-space). md-lint is not in CI, so the breakage is silent — this rule protects the LCD artifacts
  themselves.
- **WASM core behavior is verified in a real browser** — don't add native Rust tests on the
  CPU/`WasmSystem` path. The emulator's continuous worker/rAF loop means the page never reaches
  `document_idle`, so idle-gated screenshot/page-text tooling times out; drive and inspect via
  in-page JS, and sample transient UI (toasts) inside a **single** in-page async loop.

## Conventions pointer

Toolchain, test placement/discovery, and gate commands live in
`.claude/rules/lcd-conventions.md` (machine block). Decisions log: see `DECISIONS.md`.

<!--
Refresh triggers (when to re-run /lcd:onboard or let lcd:refine touch this file):
  - A new top-level zone/module appears that isn't in the table.
  - A "Don't put here" guardrail is now routinely violated (the rule moved).
  - A new surface starts being exposed.
  - An invariant is added or retired.
Keep it a map, not documentation — if a row needs a paragraph, it belongs elsewhere.
-->
