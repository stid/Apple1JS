/**
 * Global type definitions used across the application
 */

// Configuration types
export type { Config } from './config';

// Logging types
export type { 
    LogLevel, 
    LogMessage, 
    LoggingContextType 
} from './logging';

// Re-export type utilities if needed
export type {
    // Add any other global utility types here
};